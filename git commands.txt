git init { for initialization}

git status {listing all untracked file}

git add -A { adding files to staging medium}

git commit -m"your msg" {adding files to local repo}

git status

git remote add your msg https://github.com/Bismi478/gitcommands.git

git remote -v


--------------push------------


git push -u your msg master

add - commit - push {order}
